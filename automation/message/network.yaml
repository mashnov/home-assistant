## NETWORK
- alias: message_network'
  initial_state: true
  trigger:
  - platform: event
    event_type: telegram_command
    event_data:
      command: '/network'
  action:
  - service: script.message_network

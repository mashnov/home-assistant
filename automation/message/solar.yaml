## SOLAR
- alias: message_solar'
  initial_state: true
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_command
    event_data:
      command: '/solar'
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/solar'
  action:
  - service: script.message_solar

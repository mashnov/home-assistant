## NAVIGATION REMOVE
- alias: message_navigation_remove'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
  action:
  - service: script.message_navigation_remove
    data_template:
      message_id: '{{ trigger.event.data.message.message_id }}'
      chat_id: '{{ trigger.event.data.chat_id }}'

## NAVIGATION HOME
- alias: message_navigation_home'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_command
    event_data:
      command: '/home'
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/home'
  action:
  - service: script.message_navigation_home

## NAVIGATION KITCHEN
- alias: message_navigation_kitchen'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/kitchen'
  action:
  - service: script.message_navigation_kitchen

## NAVIGATION KITCHEN TOP LIGHT
- alias: message_navigation_kitchen_top_light'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/kitchen_top_light'
  action:
  - service: script.message_navigation_kitchen_top_light

## NAVIGATION KITCHEN CLIMAT
- alias: message_navigation_kitchen_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/kitchen_climat'
  action:
  - service: script.message_navigation_kitchen_climat

## NAVIGATION KITCHEN SWITCH
- alias: message_navigation_kitchen_switch'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/kitchen_switch'
  action:
  - service: script.message_navigation_kitchen_switch

## NAVIGATION BATH
- alias: message_navigation_bath'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/bath'
  action:
  - service: script.message_navigation_bath

## NAVIGATION BATH CLIMAT
- alias: message_navigation_bath_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/bath_climat'
  action:
  - service: script.message_navigation_bath_climat

## NAVIGATION TOILET
- alias: message_navigation_toilet'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/toilet'
  action:
  - service: script.message_navigation_toilet

## NAVIGATION HALL
- alias: message_navigation_hall'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/hall'
  action:
  - service: script.message_navigation_hall

## NAVIGATION HALL CLIMAT
- alias: message_navigation_hall_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/hall_climat'
  action:
  - service: script.message_navigation_hall_climat

## NAVIGATION HALL TOP LIGHT
- alias: message_navigation_hall_top_light'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/hall_top_light'
  action:
  - service: script.message_navigation_hall_top_light

## NAVIGATION BEDROOM
- alias: message_navigation_bedroom'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/bedroom'
  action:
  - service: script.message_navigation_bedroom

## NAVIGATION BEDROOM CLIMAT
- alias: message_navigation_bedroom_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/bedroom_climat'
  action:
  - service: script.message_navigation_bedroom_climat

## NAVIGATION BEDROOM TOP LIGHT
- alias: message_navigation_bedroom_top_light'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/bedroom_top_light'
  action:
  - service: script.message_navigation_bedroom_top_light

## NAVIGATION LIVING
- alias: message_navigation_living'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/living'
  action:
  - service: script.message_navigation_living

## NAVIGATION LIVING CLIMAT
- alias: message_navigation_living_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/living_climat'
  action:
  - service: script.message_navigation_living_climat

## NAVIGATION LIVING TOP LIGHT
- alias: message_navigation_living_top_light'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/living_top_light'
  action:
  - service: script.message_navigation_living_top_light

## NAVIGATION BALCONY
- alias: message_navigation_balcony'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/balcony'
  action:
  - service: script.message_navigation_balcony

## NAVIGATION BALCONY CLIMAT
- alias: message_navigation_balcony_climat'
  mode: 'queued'
  trigger:
  - platform: event
    event_type: telegram_callback
    event_data:
      command: '/balcony_climat'
  action:
  - service: script.message_navigation_balcony_climat

notification:
  automation:

###
## HOME HOMEPOD
###

  - alias: 'notification_home_homepod_doorbell_inside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_inside_action
    action:
    - service: script.homepod_message
      data_template:
        message: "Кто-то нажал кнопку звонка в коридоре!"
    - service: script.console
      data:
        message: "Parent_ID: {{ trigger.to_state.context.parent_id }} User_ID: {{ trigger.to_state.context.user_id }}"

  - alias: 'notification_home_homepod_doorbell_outside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_outside_action
    action:
    - service: script.homepod_message
      data_template:
        message: "Кто-то нажал кнопку звонка у лифта!"
    - service: script.console
      data:
        message: "Parent_ID: {{ trigger.to_state.context.parent_id }} User_ID: {{ trigger.to_state.context.user_id }}"

  - alias: 'notification_home_homepod_night_mode_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Спокойной ночи!"

  - alias: 'notification_home_homepod_night_mode_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'on'
      to: 'off'
    action:
    - service: script.homepod_message
      data_template:
        message: "Доброе утро!"

###
## HOME TELEGRAMM
###

  - alias: 'notification_home_telegramm_doorbell_inside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_inside_action
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Кто-то нажал кнопку звонка в коридоре ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_doorbell_outside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_outside_action
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Кто-то нажал кнопку звонка у лифта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_night_mode_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Ночной режим включен. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_night_mode_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Ночной режим выключен. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_intercon'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.intercom_hall_call'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text_custom_menu
      data_template:
        message: "🏠 Кто-то звонит в домофон. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"
        navigation: 
        - '🔓 Открыть :/intercom_hall_open, ❌ Закрыть :/hide_menu'

###
## CLIMAT HOMEPOD
###

  - alias: 'notification_climat_homepod_hygrostat_empty_hall'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_hall_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Закончилась вода в увлажнителе в коридоре!"

  - alias: 'notification_climat_homepod_hygrostat_empty_bedroom'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_bedroom_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Закончилась вода в увлажнителе в спальне!"

  - alias: 'notification_climat_homepod_hygrostat_empty_living'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_living_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Закончилась вода в увлажнителе в гостиной!"

  - alias: 'notification_climat_homepod_mold_kitchen'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_kitchen
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Требуется проветривание на кухне!"

  - alias: 'notification_climat_homepod_mold_hall'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_hall
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Требуется проветривание в коридоре!"

  - alias: 'notification_climat_homepod_mold_bedroom'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_bedroom
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Требуется проветривание в спальне!"

  - alias: 'notification_climat_homepod_mold_living'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_living
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Требуется проветривание в гостиной!"

###
## CLIMAT TELEGRAMM
###

  - alias: 'notification_climat_telegrammhygrostat_empty_hall'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_hall_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🧥 Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegrammhygrostat_empty_bedroom'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_bedroom_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛏️ Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegrammhygrostat_empty_living'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_living_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛋️ Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_kitchen'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_kitchen
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🥣 Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_hall'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_hall
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🧥 Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_bedroom'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_bedroom
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛏️ Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_living'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_living
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛋️ Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

###
## SYSTEM
###

  - alias: 'notification_system_shutdown'
    mode: 'queued'
    trigger:
    - platform: homeassistant
      event: shutdown
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Завершение работы ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_start'
    mode: 'queued'
    trigger:
    - platform: homeassistant
      event: start
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Старт системы ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_backup'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.system_is_backed_up'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Резервное копирование ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_backup_error'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'sensor.backup_state'
      to: 'error'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Ошибка резервного копирования ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_power_error'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'binary_sensor.rpi_power_status'
      from: 'off'
      to: 'on'
      for:
        seconds: 5
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Проблемы с питанием ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_file_editor'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.file_editor_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление FILE EDITOR ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_home_assistant_core'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.home_assistant_core_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление HOME ASSISTANT CORE ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_google_drive_backup'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.home_assistant_google_drive_backup_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление GOOGLE DRIVE BACKUP ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_home_assistant'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.home_assistant_operating_system_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление HOME ASSISTANT ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_supervisor'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.home_assistant_supervisor_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление SUPERVISOR ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_mosquitto_broker'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.mosquitto_broker_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление MOSQUITO BROCKER ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_tailscale'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.tailscale_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление TAILSCALE ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_zigbee2mqtt'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.zigbee2mqtt_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление ZIGBEE 2 MQTT ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update_terminal_ssh'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.terminal_ssh_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление TERMINAL & SSH ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_hacs_update'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.hacs
      above: 0
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление HACS ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

###
## SECURITY
###

  - alias: 'notification_secuirity_balcony_door_open'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_balcony_virtual'
      from: 'off'
      to: 'on'
    action:
    # - service: script.camera_living_take_photo
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🪴 Дверь открыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_door_close'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_balcony_virtual'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🪴 Дверь закрыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_hall_door_open'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_hall_virtual'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🧥 Дверь открыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_hall_door_close'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_hall_virtual'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🧥 Дверь закрыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

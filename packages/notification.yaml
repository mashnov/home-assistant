notification:
  automation:

###
## HOME HOMEPOD
###

  - alias: 'notification_home_homepod_doorbell_inside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_inside_action
    action:
    - service: script.homepod_message
      data_template:
        message: "Кто-то нажал кнопку звонка в кооидоре"

  - alias: 'notification_home_homepod_doorbell_outside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_outside_action
    action:
    - service: script.homepod_message
      data_template:
        message: "Кто-то нажал кнопку звонка у лифта"

  - alias: 'notification_home_homepod_night_mode_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Спокойной ночи."

  - alias: 'notification_home_homepod_night_mode_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'on'
      to: 'off'
    action:
    - service: script.homepod_message
      data_template:
        message: "Доброе утро!"

  - alias: 'notification_home_homepod_blocker_top_light_kitchen_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_kitchen_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить свет на кухне"

  - alias: 'notification_home_homepod_blocker_back_light_kitchen_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.back_light_kitchen_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить подсветку на кухне"

  - alias: 'notification_home_homepod_blocker_top_light_hall_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_hall_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить свет в коридоре"

  - alias: 'notification_home_homepod_blocker_top_light_bath_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_bath_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить свет в ванной"

  - alias: 'notification_home_homepod_blocker_night_light_bath_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.night_light_bath_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить зеркало в ванной"

  - alias: 'notification_home_homepod_blocker_fan_toilet_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.fan_toilet_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.homepod_message
      data_template:
        message: "Не забудь выключить вытяжку"

###
## HOME TELEGRAMM
###

  - alias: 'notification_home_telegramm_doorbell_inside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_inside_action
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Кто-то нажал кнопку звонка в кооидоре ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_doorbell_outside'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: event.doorbell_outside_action
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Кто-то нажал кнопку звонка у лифта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_night_mode_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Ночной режим включен. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_telegramm_night_mode_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.is_night_mode'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🏠 Ночной режим выключен. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_kitchen_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_kitchen_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🥣 Свет включен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_kitchen_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_kitchen_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🥣 Свет выключен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_back_light_kitchen_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.back_light_kitchen_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🥣 Подсветка включена ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_back_light_kitchen_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.back_light_kitchen_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🥣 Подсветка выключена ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_hall_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_hall_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🧥 Свет включен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_hall_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_hall_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🧥 Свет выключен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_bath_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_bath_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🛁 Свет включен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_top_light_bath_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.top_light_bath_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🛁 Свет выключен ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_night_light_bath_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.night_light_bath_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🛁 Зеркало включено ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_night_light_bath_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.night_light_bath_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🛁 Зеркало выключено ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_fan_toilet_on'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.fan_toilet_blocker'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🚽 Вытяжка включена ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_home_homepod_blocker_fan_toilet_off'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'input_boolean.fan_toilet_blocker'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_home_text
      data_template:
        message: "🚽 Вытяжка выключена ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

###
## CLIMAT HOMEPOD
###

  - alias: 'notification_climat_homepod_hygrostat_empty_hall'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_hall_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Коридор. Закончилась вода в увлажнителе"

  - alias: 'notification_climat_homepod_hygrostat_empty_bedroom'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_bedroom_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Спальня. Закончилась вода в увлажнителе"

  - alias: 'notification_climat_homepod_hygrostat_empty_living'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_living_is_empty'
      from: 'off'
      to: 'on'
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Гостиная. Закончилась вода в увлажнителе"

  - alias: 'notification_climat_homepod_mold_kitchen'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_kitchen
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Кухня. Требуется проветривание."

  - alias: 'notification_climat_homepod_mold_hall'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_hall
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Коридор. Требуется проветривание."

  - alias: 'notification_climat_homepod_mold_bedroom'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_bedroom
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Спальня. Требуется проветривание."

  - alias: 'notification_climat_homepod_mold_living'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_living
      above: 70
    action:
    - choose:
      - conditions:
        - condition: state
          entity_id: input_boolean.is_night_mode
          state: 'off'
        sequence:
        - service: script.homepod_message
          data_template:
            message: "Гостиная. Требуется проветривание."

###
## CLIMAT TELEGRAMM
###

  - alias: 'notification_climat_telegrammhygrostat_empty_hall'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_hall_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🧥 Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegrammhygrostat_empty_bedroom'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_bedroom_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛏️ Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegrammhygrostat_empty_living'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.hygrostat_living_is_empty'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛋️ Закончилась вода в увлажнителе ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_kitchen'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_kitchen
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🥣 Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_hall'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_hall
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🧥 Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_bedroom'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_bedroom
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛏️ Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_climat_telegramm_mold_living'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.mold_living
      above: 70
    action:
    - service: script.telegramm_send_climat_text
      data_template:
        message: "🛋️ Требуется проветривание. ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

###
## SYSTEM
###

  - alias: 'notification_system_shutdown'
    mode: 'queued'
    trigger:
    - platform: homeassistant
      event: shutdown
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Завершение работы ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_start'
    mode: 'queued'
    trigger:
    - platform: homeassistant
      event: start
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Старт системы ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_backup'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.system_is_backed_up'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Резервное копирование ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_backup_error'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'sensor.backup_state'
      to: 'error'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Ошибка резервного копирования ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_power_error'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'binary_sensor.rpi_power_status'
      from: 'off'
      to: 'on'
      for:
        seconds: 5
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Проблемы с питанием ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_update'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.home_assistant_core_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.home_assistant_operating_system_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.home_assistant_supervisor_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление системы ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_plugin_update'
    mode: 'queued'
    trigger:
    - platform: state
      entity_id: 'update.tailscale_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.zigbee2mqtt_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.mosquitto_broker_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.home_assistant_google_drive_backup_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.file_editor_update'
      from: 'off'
      to: 'on'
    - platform: state
      entity_id: 'update.terminal_ssh_update'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление расширений ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_system_hacs_update'
    mode: 'queued'
    trigger:
    - platform: numeric_state
      entity_id: sensor.hacs
      above: 0
    action:
    - service: script.telegramm_send_system_text
      data_template:
        message: "⚙️ Доступно обновление HACS ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

###
## SECURITY
###

  - alias: 'notification_secuirity_balcony_door_open'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_balcony'
      from: 'off'
      to: 'on'
    action:
    - service: script.camera_living_take_photo
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🪴 Дверь открыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_door_close'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_balcony'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🪴 Дверь закрыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_hall_door_open'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_hall'
      from: 'off'
      to: 'on'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🧥 Дверь открыта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

  - alias: 'notification_secuirity_hall_door_close'
    mode: 'queued'
    trigger:
      platform: state
      entity_id: 'binary_sensor.door_hall'
      from: 'on'
      to: 'off'
    action:
    - service: script.telegramm_send_secuirity_text
      data_template:
        message: "🧥 Дверь закрта ({{ now().timestamp() | timestamp_custom('%H:%M') }})"

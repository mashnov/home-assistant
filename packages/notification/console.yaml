notification_console:
  script:
    console:
      mode: 'queued'
      sequence:
      - service: persistent_notification.create
        data:
          title: 'УВЕДОМЛЕНИЕ'
          message: "{{ message }}"

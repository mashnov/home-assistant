notification_homepod:
  script:
    homepod_message:
      mode: 'queued'
      sequence:
      - service: tts.sveta_say
        data:
          entity_id: media_player.homepod
          message: "{{ message }}"
          cache: false
intercom:
  lock:
  - platform: template
    name: intercom_hall
    value_template: "{{ is_state('switch.intercom_hall', 'off') }}"
    lock:
    - service: switch.turn_off
      entity_id: switch.intercom_hall
    unlock:
    - service: script.intercom_hall_open

  input_boolean:
    intercom_hall:
    intercom_hall_auto_on:

  script:
    intercom_hall_open:
      mode: 'queued'
      sequence:
      - service: script.intercom_hall_open
        data:
          message: 'ДОМОФОН'


      # - service: switch.turn_on
      #   entity_id: switch.intercom_hall
      # - delay: 00:00:15
      # - service: switch.turn_off
      #   entity_id: switch.intercom_hall

    
cover_living:
  cover:
  - platform: template
    covers:
      cover_living:
        device_class: curtain
        value_template: "{{ state_attr('cover.curtain_living', 'position') | float > 0 }}"
        position_template: "{{ state_attr('cover.curtain_living', 'position') }}"
        open_cover:
        - service: cover.open_cover
          entity_id: cover.curtain_living
        close_cover:
        - service: cover.close_cover
          entity_id: cover.curtain_living
        stop_cover:
        - service: cover.stop_cover
          entity_id: cover.curtain_living
        set_cover_position:
        - service: cover.set_cover_position
          entity_id: cover.curtain_living
          data:
            position: "{{ position }}"

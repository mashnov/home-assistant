automation_toilet_back_light:
  automation:
  - alias: automation_toilet_back_light_on
    mode: queued
    trigger:
    - platform: state
      entity_id: binary_sensor.occupancy_toilet
      from: "off"
      to: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_back_toilet

  - alias: automation_toilet_back_light_off
    mode: queued
    trigger:
    - platform: state
      entity_id: binary_sensor.occupancy_toilet
      from: "on"
      to: "off"
    action:
    - service: light.turn_off
      entity_id: light.light_back_toilet

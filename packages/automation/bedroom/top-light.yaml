automation_bedroom_top_light:
  automation:
  - alias: automation_bedroom_top_light_toggle
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.cube_bedroom_action
      to: flip180
    action:
    - service: light.toggle
      entity_id: light.light_top_bedroom

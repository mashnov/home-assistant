automation_hall_xiaomi:
  automation:
  - alias: automation_hall_xiaomi_1_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_1
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_1
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_1
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

  - alias: automation_hall_xiaomi_2_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_2
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_2
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_2
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

  - alias: automation_hall_xiaomi_3_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_3
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_3
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_3
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

  - alias: automation_hall_xiaomi_4_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_4
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_4
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_4
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

  - alias: automation_hall_xiaomi_5_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_5
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_5
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_5
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

  - alias: automation_hall_xiaomi_6_param
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.light_top_hall_brightness
    - platform: state
      entity_id: sensor.light_top_hall_temperature
    - platform: state
      entity_id: light.light_xiaomi_hall_6
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: light.light_xiaomi_hall_6
      state: "on"
    action:
    - service: light.turn_on
      entity_id: light.light_xiaomi_hall_6
      data_template:
        brightness: "{{ states('sensor.light_top_hall_brightness') int(50) }}"
        color_temp: "{{ states('sensor.light_top_hall_temperature') | int(333) }}"

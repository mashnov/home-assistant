automation_kitchen_top_light:
  automation:
  - alias: automation_kitchen_top_light_on
    mode: queued
    trigger:
    - platform: state
      entity_id: binary_sensor.occupancy_kitchen
      from: "off"
      to: "on"
    condition:
    - condition: state
      entity_id: input_boolean.is_night_mode
      state: "off"
    - condition: numeric_state
      entity_id: sensor.light_param_illuminance_virtual
      below: 1000
    action:
    - service: light.turn_on
      entity_id: light.light_top_kitchen

  - alias: automation_kitchen_top_light_off
    mode: queued
    trigger:
    - platform: state
      entity_id: binary_sensor.occupancy_kitchen
      from: "on"
      to: "off"
    action:
    - service: light.turn_off
      entity_id: light.light_top_kitchen

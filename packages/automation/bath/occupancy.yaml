automation_bath_occupancy:
  automation:
  - alias: automation_bath_occupancy_toggle
    mode: queued
    trigger:
    - platform: state
      entity_id: sensor.restroom_switch_action
      to: "single_left"
    action:
    - service: input_boolean.toggle
      entity_id: input_boolean.occupancy_bath

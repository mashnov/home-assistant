switch_kitchen:
  switch:
  - platform: template
    switches:
      switch_kitchen:
        value_template: >
          {% set left = is_state('binary_sensor.switch_kitchen_left', 'on') %}
          {% set right = is_state('binary_sensor.switch_kitchen_right', 'on') %}    
          {{ left and right }}
        turn_on:
        - service: switch.turn_on
          data:
            entity_id: 
            - switch.switch_kitchen_left 
            - switch.switch_kitchen_right
        turn_off:
        - service: switch.turn_off
          data:
            entity_id: 
            - switch.switch_kitchen_left
            - switch.switch_kitchen_right

switch_living:
  switch:
  - platform: template
    switches:
      switch_living:
        value_template: >
          {% set left = is_state('binary_sensor.switch_living_left', 'on') %}
          {% set right = is_state('binary_sensor.switch_living_right', 'on') %}    
          {{ left and right }}
        turn_on:
        - service: switch.turn_on
          data:
            entity_id: 
            - switch.switch_living_left
            - switch.switch_living_right
        turn_off:
        - service: switch.turn_off
          data:
            entity_id: 
            - switch.switch_living_left
            - switch.switch_living_right

dormell_hall:
  camera:
  - platform: generic
    name: doorbell_outside
    still_image_url: null

  - platform: generic
    name: doorbell_inside
    still_image_url: null

  template:
  - binary_sensor:
    - name: 'doorbell_outside'
      state: "off"

    - name: 'doorbell_inside'
      state: "off"

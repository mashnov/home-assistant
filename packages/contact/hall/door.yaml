contact_hall_door:
  template:
  - binary_sensor:
    - name: contact_hall_door_virtual
      unique_id: contact_hall_door_virtual
      device_class: door
      state: "{{ is_state('binary_sensor.door_hall', 'on') }}"

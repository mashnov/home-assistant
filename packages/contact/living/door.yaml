contact_living_door:
  template:
  - binary_sensor:
    - name: contact_living_door_virtual
      unique_id: contact_living_door_virtual
      device_class: door
      state: "{{ is_state('binary_sensor.door_living', 'on') }}"

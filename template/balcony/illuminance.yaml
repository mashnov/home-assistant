- sensor:
  - name: 'i_balcony'
    unit_of_measurement: 'lx'
    device_class: 'illuminance'
    icon: 'mdi:white-balance-sunny'
    state: >
      {% set lastValue = states('sensor.i_balcony_last') | int(0) %}
      {{ states('sensor.balcony_illuminance') | float(lastValue) | round(1) }}
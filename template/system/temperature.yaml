- sensor:
  - name: 't_average'
    unit_of_measurement: '°C'
    device_class: 'temperature'
    icon: >
      {% set lIcon = 'mdi:snowflake' %}
      {% set nIcon = 'mdi:check-decagram-outline' %}
      {% set hIcon = 'mdi:fire' %}
      {% set isLow = is_state('binary_sensor.t_average_is_low', 'on') %}
      {% set isNormal = is_state('binary_sensor.t_average_is_normal', 'on') %}
      {{ iif(isNormal, nIcon, iif(isLow, lIcon, hIcon)) }}
    state: >
      {% set kitchen = states('sensor.t_bath') | float(0) %}
      {% set hall = states('sensor.t_bath') | float(0) %}
      {% set bedroom = states('sensor.t_bath') | float(0) %}
      {% set living = states('sensor.t_bath') | float(0) %}
      {{ ((kitchen + hall + bedroom + living) / 4) | round(1) }}

- binary_sensor:
  - name: 't_average_is_normal'
    device_class: window
    state: "{{ is_state('binary_sensor.t_average_is_low', 'off') and is_state('binary_sensor.t_average_is_hight', 'off') }}"

  - name: 't_average_is_low'
    state: >
      {% set sensorValue = states('sensor.t_average') | float %}
      {% set normalValue = states('input_number.t_target') | int %}
      {% set offsetValue = states('input_number.t_offset') | int %}
      {% set targetValue = normalValue - offsetValue %}
      {{ sensorValue < targetValue }}

  - name: 't_average_is_hight'
    state: >
      {% set sensorValue = states('sensor.t_average') | float %}
      {% set normalValue = states('input_number.t_target') | int %}
      {% set offsetValue = states('input_number.t_offset') | int %}
      {% set targetValue = normalValue + offsetValue %}
      {{ sensorValue > targetValue }}
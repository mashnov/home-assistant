- sensor:
  - name: 'network'
    device_class: 'data_rate'
    unit_of_measurement: Mbit/s
    state: "{{ state_attr('sensor.network', 'download') }}"
    icon: >
      {% set isNormalIcon = state_attr('sensor.network', 'normal_state') and 'mdi:speedometer' %}
      {% set isLowIcon = not(isNormalIcon) and 'mdi:speedometer-slow' %}
      {% set defaultIcon = 'mdi:help-circle' %}
      {{ isNormalIcon or isLowIcon or defaultIcon }}
    attributes:
      ping_sensor: "{{ states('sensor.speedtest_ping') }}"
      upload_sensor: "{{ states('sensor.speedtest_upload') }}"
      download_sensor: "{{ states('sensor.speedtest_download') }}"
      
      normal_state: >
        {% set sensorValue = state_attr('sensor.network', 'download') | int(0) %}
        {% set normalValue = states('input_number.n_target') | int(0) %}
        {% set offsetValue = states('input_number.n_offset') | int(0) %}
        {% set targetValue = normalValue - offsetValue %}
        {{ sensorValue > targetValue }}

      normal_boolean: "{{ is_state('input_boolean.n_normal', 'on') }}"

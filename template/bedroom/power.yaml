- sensor:
  - name: 'pw_bedroom'
    unit_of_measurement: 'W'
    device_class: 'power'
    state: >
      {% set lastValue = states('sensor.pw_bedroom_last') | int %}
      {{ states('sensor.bedroom_relay_power') | float(lastValue) | round(1) }}
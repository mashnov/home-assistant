- sensor:
  - name: 'date-time'
    state: "{{ now() }}"
    device_class: 'date'
    icon: "mdi:calendar-clock"
    attributes:
      time: '{{ as_timestamp(now()) | timestamp_custom("%H:%M") }}'
      date: '{{ as_timestamp(now()) | timestamp_custom("%d %m %Y") }}'
      date_time: '{{ as_timestamp(now()) | timestamp_custom("%d %m %Y %H:%M") }}'
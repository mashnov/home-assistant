- sensor:
  - name: 'pw_hall'
    unit_of_measurement: 'W'
    device_class: 'power'
    state: >
      {% set lastValue = states('sensor.pw_hall_last') | int %}
      {{ states('sensor.hall_relay_power') | float(lastValue) | round(1) }}
- sensor:
  - name: 't_hall'
    unit_of_measurement: '°C'
    device_class: 'temperature'
    state: "{{ states('sensor.hall_temperature') | float(0) | round(2) }}"

- binary_sensor:
  - name: 't_hall_is_normal'
    device_class: window
    state: "{{ is_state('binary_sensor.t_hall_is_low', 'off') and is_state('binary_sensor.t_hall_is_hight', 'off') }}"

  - name: 't_hall_is_low'
    state: >
      {% set sensorValue = states('sensor.t_hall') | float %}
      {% set normalValue = states('input_number.in_t_hall') | int %}
      {% set offsetValue = states('input_number.in_t_offset') | int %}
      {% set targetValue = normalValue - offsetValue %}
      {{ sensorValue < targetValue }}

  - name: 't_hall_is_hight'
    state: >
      {% set sensorValue = states('sensor.t_hall') | float %}
      {% set normalValue = states('input_number.in_t_hall') | int %}
      {% set offsetValue = states('input_number.in_t_offset') | int %}
      {% set targetValue = normalValue + offsetValue %}
      {{ sensorValue > targetValue }}
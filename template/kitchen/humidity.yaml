- sensor:
  - name: 'h_kitchen'
    unit_of_measurement: '%'
    device_class: 'humidity'
    state: "{{ states('sensor.kitchen_humidity') | float(0) | round(1) }}"
    attributes:
      isNormal: >
        {% set isLow = state_attr('sensor.h_kitchen', 'isLow') %}
        {% set isHight = state_attr('sensor.h_kitchen', 'isHight') %}
        {{ not(isLow) and not(isHight) }}
      isHight: >
        {% set sensorValue = states('sensor.h_kitchen') | float(0) %}
        {% set normalValue = states('input_number.h_target') | int(0) %}
        {% set offsetValue = states('input_number.h_offset') | int(0) %}
        {% set targetValue = normalValue + offsetValue %}
        {{ sensorValue > targetValue }}
      isLow: >
        {% set sensorValue = states('sensor.h_kitchen') | float(0) %}
        {% set normalValue = states('input_number.h_target') | int(0) %}
        {% set offsetValue = states('input_number.h_offset') | int(0) %}
        {% set targetValue = normalValue - offsetValue %}
        {{ sensorValue < targetValue }}
    icon: >
      {% set isNormal = state_attr('sensor.h_kitchen', 'isNormal') %}
      {% set isLow = state_attr('sensor.h_kitchen', 'isLow') %}
      {% set lIcon = 'mdi:water-outline' %}
      {% set nIcon = 'mdi:water-opacity' %}
      {% set hIcon = 'mdi:water' %}
      {{ iif(isNormal, nIcon, iif(isLow, lIcon, hIcon)) }}

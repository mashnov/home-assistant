back_light_bedroom_1_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.bedroom_relay_l1'

back_light_bedroom_1_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.bedroom_relay_l1'

back_light_bedroom_2_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.bedroom_wall_switch'

back_light_bedroom_2_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.bedroom_wall_switch'

back_light_kitchen_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.kitchen_relay_l2'

back_light_kitchen_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.kitchen_relay_l2'

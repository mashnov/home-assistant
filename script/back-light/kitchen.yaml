bl_kitchen_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.kitchen_relay_l2'

bl_kitchen_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.kitchen_relay_l2'

bl_kitchen_toggle:
  mode: 'queued'
  sequence:
  - service: switch.toggle
    entity_id: 'switch.kitchen_relay_l2'

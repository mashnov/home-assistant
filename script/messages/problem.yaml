## PROBLEM
### NETWORK
network_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        🟠 Соединение .......... {{ state_attr('binary_sensor.date_time', 'hm') }} UTC
        ⬇️ Средняя ................. {{ state_attr('binary_sensor.problem_network', 'download') }}
        ⬆️ Кухня ..................... {{ state_attr('binary_sensor.problem_network', 'upload') }}
        🔀 Ванная ................... {{ state_attr('binary_sensor.problem_network', 'ping') }}

### EMPTY_HUMIDIFIER
problem_empty_hall_low_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        ⏱️ {{ state_attr('sensor.date_time', 'hm') }} UTC
        🧥 Коридор. Закончилась вода в увлажнителе

problem_empty_bedroom_low_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        ⏱️ {{ state_attr('sensor.date_time', 'hm') }} UTC
        🛌 Спальня. Закончилась вода в увлажнителе

problem_empty_living_low_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        ⏱️ {{ state_attr('sensor.date_time', 'hm') }} UTC
        📚 Гостиная. Закончилась вода в увлажнителе
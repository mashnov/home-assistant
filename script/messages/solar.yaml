## SOLAR
solar_hight_on_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        ⏱️ {{ state_attr('sensor.date_time', 'hmz') }}
        🟠 Солнце попадает в окна

solar_hight_off_message:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: | 
        ⏱️ {{ state_attr('sensor.date_time', 'hmz') }}
        🟢 Солнце не попадает в окна

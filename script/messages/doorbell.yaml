## MESSAGE DORBELL
message_doorbell_elevator:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: '🛎️ Коридор. Нажата кнопка звонка {{ state_attr("sensor.date_time", "hm") }} UTC'
      data:
        inline_keyboard: 
        - '❌ :/hide_menu'

message_doorbell_hall:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: '🛎️ Прихожая. Нажата кнопка звонка {{ state_attr("sensor.date_time", "hm") }} UTC'
      data:
        inline_keyboard: 
        - '❌ :/hide_menu'

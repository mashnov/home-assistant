## MESSAGE DORBELL
message_doorbell_elevator:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: "⏱️ {{ state_attr('sensor.date_time', 'hm') }} UTC"
      data:
        inline_keyboard: 
        - '🛎️ Коридор. Нажата кнопка звонка :/hide_menu'

message_doorbell_hall:
  mode: 'queued'
  sequence:
  - service: notify.telegram_home_group_id
    data_template:
      message: "⏱️ {{ state_attr('sensor.date_time', 'hm') }} UTC"
      data:
        inline_keyboard: 
        - '🛎️ Прихожая. Нажата кнопка звонка :/hide_menu'

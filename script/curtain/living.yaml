c_living_open:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ states("input_number.c_living_open") }}'

c_living_close:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ states("input_number.c_living_close") }}'

c_living_sun_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ states("input_number.c_living_sun") }}'

c_living_night_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ states("input_number.c_living_night") }}'

c_living_ventilation_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ states("input_number.c_living_ventilation") }}'

c_living_set_position:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_living'
    data_template:
      position: '{{ position }}'

c_bedroom_open:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ states("input_number.c_bedroom_open") }}'

c_bedroom_close:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ states("input_number.c_bedroom_close") }}'

c_bedroom_sun_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ states("input_number.c_bedroom_sun") }}'

c_bedroom_night_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ states("input_number.c_bedroom_night") }}'

c_bedroom_ventilation_mode:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ states("input_number.c_bedroom_ventilation") }}'

c_bedroom_set_position:
  mode: 'queued'
  sequence:
  - service: cover.set_cover_position
    entity_id: 'cover.curtain_bedroom'
    data_template:
      position: '{{ position }}'

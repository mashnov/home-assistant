## CLOUD LEVEL
c_cloud_set_number:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.c_level
    data_template:
      value: "{{ states('sensor.cloud-level') }}"



## NETWORK
set_n_download_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.n_download_level
    data_template:
      value: "{{ states('sensor.n_download_level') | float(0) }}"

set_n_upload_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.n_upload_level
    data_template:
      value: "{{ states('sensor.n_upload_level') | float(0) }}"

set_n_ping_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.n_ping_level
    data_template:
      value: "{{ states('sensor.n_ping_level') | float(0) }}"

## PRESSURE
set_p_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_level
    data_template:
      value: "{{ states('sensor.p_level') | float(0) }}"

set_p_kitchen_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_kitchen_level
    data_template:
      value: "{{ states('sensor.p_kitchen_level') | float(0) }}"

set_p_bath_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_bath_level
    data_template:
      value: "{{ states('sensor.p_bath_level') | float(0) }}"

set_p_hall_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_hall_level
    data_template:
      value: "{{ states('sensor.p_hall_level') | float(0) }}"

set_p_bedroom_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_bedroom_level
    data_template:
      value: "{{ states('sensor.p_bedroom_level') | float(0) }}"

set_p_living_level:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.p_living_level
    data_template:
      value: "{{ states('sensor.p_living_level') | float(0) }}"

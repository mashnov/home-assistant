## ILLUMINANCE
i_level_set_number:
  mode: 'queued'
  sequence:
  - service: input_number.set_value
    entity_id: input_number.i_level
    data_template:
      value: "{{ states('sensor.illuminance') }}"

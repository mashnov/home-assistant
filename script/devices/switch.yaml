## SWITCH KITCHEN LEFT
switch_kitchen_left_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 
    - 'switch.kitchen_switch_left'

switch_kitchen_left_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 
    - 'switch.kitchen_switch_left'


## SWITCH KITCHEN RIGHT
switch_kitchen_right_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 
    - 'switch.kitchen_switch_right'

switch_kitchen_right_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 
    - 'switch.kitchen_switch_right'


## SWITCH HALL
switch_hall_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.hall_switch'

switch_hall_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.hall_switch'


## SWITCH BEDROOM
switch_bedroom_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.bedroom_switch'

switch_bedroom_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.bedroom_switch'


## SWITCH LIVING
switch_living_on:
  mode: 'queued'
  sequence:
  - service: switch.turn_on
    entity_id: 'switch.living_switch'

switch_living_off:
  mode: 'queued'
  sequence:
  - service: switch.turn_off
    entity_id: 'switch.living_switch'

## KITCHEN
w_kitchen_left_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.w_kitchen_left'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'window') }}"

w_kitchen_left_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.w_kitchen_left'

w_kitchen_right_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.w_kitchen_right'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'window') }}"

w_kitchen_right_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.w_kitchen_right'


## BEDROOM
w_bedroom_left_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.w_bedroom_left'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'window') }}"

w_bedroom_left_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.w_bedroom_left'

w_bedroom_right_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.w_bedroom_right'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'window') }}"

w_bedroom_right_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.w_bedroom_right'


## LIVING
w_living_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.w_living'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'window') }}"

w_living_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.w_living'
## KITCHEN
o_kitchen_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.o_kitchen'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'occupation') }}"

o_kitchen_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.o_kitchen'

## HALL
o_hall_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.o_hall'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'occupation') }}"

o_hall_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.o_hall'


## BATH
o_bath_timer_start:
  mode: 'queued'
  sequence:
  - service: timer.start
    entity_id: 'timer.o_bath'
    data_template:
      duration: "{{ state_attr('sensor.delay', 'occupation') }}"

o_bath_timer_cancel:
  mode: 'queued'
  sequence:
  - service: timer.cancel
    entity_id: 'timer.o_bath'
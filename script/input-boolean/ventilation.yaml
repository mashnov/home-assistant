## VENTILATION KITCHEN
ventilation_kitchen_on:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.ventilation_kitchen

ventilation_kitchen_off:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.ventilation_kitchen

## HALL KITCHEN
ventilation_hall_on:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.ventilation_hall

ventilation_hall_off:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.ventilation_hall

## BEDROOM KITCHEN
ventilation_bedroom_on:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.ventilation_bedroom

ventilation_bedroom_off:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.ventilation_bedroom

## LIVING KITCHEN
ventilation_living_on:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.ventilation_living

ventilation_living_off:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.ventilation_living

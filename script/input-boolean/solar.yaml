## AZIMUTH
on_s_azimuth_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.s_azimuth_state

off_s_azimuth_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.s_azimuth_state


## ELEVATION
on_s_elevation_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.s_elevation_state

off_s_elevation_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.s_elevation_state


## HIGHT
on_s_hight_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.s_hight_state

off_s_hight_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.s_hight_state


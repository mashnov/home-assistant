## HIGHT
on_t_hight_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.t_hight_state

off_t_hight_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.t_hight_state


## LOW
on_t_low_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.t_low_state

off_t_low_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.t_low_state

## NETWORK
on_n_upload_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.n_upload_state

off_n_upload_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.n_upload_state

on_n_download_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.n_download_state

off_n_download_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.n_download_state

on_n_ping_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_on
    entity_id: input_boolean.n_ping_state

off_n_ping_state:
  mode: 'queued'
  sequence:
  - service: input_boolean.turn_off
    entity_id: input_boolean.n_ping_state

## HUMIDITY
notification_humidifier_empty_hall:
  mode: 'queued'
  sequence:
  - service: tts.sveta_say
    data:
      entity_id: media_player.homepod
      message: "{{ states('input_text.problem_empty_hall') }}"
      cache: true

notification_humidifier_empty_bedroom:
  mode: 'queued'
  sequence:
  - service: tts.sveta_say
    data:
      entity_id: media_player.homepod
      message: "{{ states('input_text.problem_empty_bedroom') }}"
      cache: true

notification_humidifier_empty_living:
  mode: 'queued'
  sequence:
  - service: tts.sveta_say
    data:
      entity_id: media_player.homepod
      message: "{{ states('input_text.problem_empty_living') }}"
      cache: true

## DORBELL
notification_doorbell_elevator:
  mode: 'queued'
  sequence:
  - service: tts.sveta_say
    data:
      entity_id: media_player.homepod
      message: "{{ states('input_text.doorbell_elevator') }}"
      cache: true

notification_doorbell_hall:
  mode: 'queued'
  sequence:
  - service: tts.sveta_say
    data:
      entity_id: media_player.homepod
      message: "{{ states('input_text.doorbell_hall') }}"
      cache: true
